import CountryList from "../components/country/countrylist/CountryList";

const Countries = () => {
  // render
  return (
    <div>
      <CountryList />
    </div>
  );
};

export default Countries;
